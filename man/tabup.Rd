% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabup.R
\name{tabup}
\alias{tabup}
\title{Cross Tabulation Function}
\usage{
tabup(x, ..., wt = NULL, na.rm = FALSE, sort = TRUE)
}
\arguments{
\item{x}{A data.frame or tibble.}

\item{...}{Variables to include in the tabulation. If only one variable is provided, a total row is added to the output.}

\item{wt}{Optional. A variable providing frequency weights. Defaults to \code{NULL}.}

\item{na.rm}{Logical. Should missing values (\code{NA}) be removed before computation? Defaults to \code{FALSE}.}

\item{sort}{Logical. Should the output be sorted by the grouping variables? Defaults to \code{TRUE}.}
}
\value{
A data.frame sorted by the specified variables, including columns \code{Freq}, \code{Percent}, and \code{Cum} for counts.
}
\description{
Generates a frequency table for the specified variables, including counts, percentages, and cumulative percentages. Supports weighted counts and optional removal of missing values.
}
\examples{
# Setup
library(dplyr)
N <- 100
K <- 10
df <- tibble(
  id = sample(c(NA, 1:5), N / K, TRUE),
  v1 = sample(1:5, N / K, TRUE)
)
# One-way tabulation
df \%>\% tabup(id)
df \%>\% tabup(id, wt = v1)
# Two-way tabulation
df \%>\% tabup(id, v1)
df \%>\% filter(id >= 3) \%>\% tabup(id)
}
